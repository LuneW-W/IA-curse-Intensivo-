hay que crear un entorno virtual nuevo con cada nuevo computador al cual sincronisemos el proyecto
para recuperar los repositorios

    pip install -r reqirements.txt

reqirements.txt es un archivo que hizo el profesor el cual contiene las librerias que necesita del proyecto



